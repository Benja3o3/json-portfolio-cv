---
import { work } from "@cv";
---

<section class="flex flex-col gap-1 mr-1">
    <h1 class="text-xl font-semibold">Experiencia</h1>
    {
        work.map((w) => (
            <div class="flex flex-col gap-1 experiencia-item">
                <h2 class="text-lg font-semibold bg-gray-300">{w.position}</h2>
                <div class="flex flex-row justify-between bg-gray-200">
                    <p class="text-sm">{w.name}</p>
                    <p class="text-sm">
                        {w.startDate} / {w.endDate}
                    </p>
                </div>
                <p class="text-sm">{w.summary}</p>
                <ul class="list-disc pl-5 flex flex-col gap-1 experiencia-highlights">
                    {w.highlights.map((h) => (
                        <li class="text-sm text-gray-800">{h}</li>
                    ))}
                </ul>
            </div>
        ))
    }
</section>

<style>
    @media print {
        /* Evita el salto de página solo en las experiencias individuales */
        .experiencia-item {
            page-break-inside: avoid;
        }

        /* Permite que las listas y otros elementos fluyan normalmente */
        .experiencia-highlights {
            page-break-inside: auto;
        }
    }
</style>
